<?xml version="1.0" encoding="UTF-8" ?>
<solver>
	<moveThreadCount>AUTO</moveThreadCount>
    <solutionClass>at.jku.dke.slotmachine.optimizer.optimization.benchmark.FlightPrioritization</solutionClass>
    <entityClass>at.jku.dke.slotmachine.optimizer.optimization.benchmark.FlightBenchmark</entityClass>
    <!-- exhaustive search for BRUTE FORCE
      <exhaustiveSearch>
		<exhaustiveSearchType>BRUTE_FORCE</exhaustiveSearchType>
	  </exhaustiveSearch>-->
     <!-- exhaustive search for BRANCH AND BOUND -->
       <exhaustiveSearch>
	    <exhaustiveSearchType>BRANCH_AND_BOUND</exhaustiveSearchType>
	  </exhaustiveSearch>
    <scoreDirectorFactory>
        <easyScoreCalculatorClass>at.jku.dke.slotmachine.optimizer.optimization.benchmark.FlightPrioritizationEasyScoreCalculator</easyScoreCalculatorClass>
    </scoreDirectorFactory>
    <!-- <termination>
    	<terminationCompositionStyle>AND</terminationCompositionStyle>
    	<bestScoreFeasible>true</bestScoreFeasible> looks only for a feasible solution	
    	<unimprovedSecondsSpentLimit>300</unimprovedSecondsSpentLimit>
    </termination>-->
    <environmentMode>REPRODUCIBLE</environmentMode>
</solver>